/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  basePath: '/internett', // Replace with your GitHub repository name
  assetPrefix: '/internett/', // Replace with your GitHub repository name
}

module.exports = nextConfig
